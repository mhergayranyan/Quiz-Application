<resources>

    <!-- App Base Theme -->
    <style name="Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/black</item>
    </style>



    <style name="AppTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textColor">?android:attr/textColorPrimary</item>
    </style>



    <style name="BottomNavText">
        <item name="android:textSize">12sp</item>
        <item name="itemIconSize">24dp</item> <!-- Default is 24dp but good to specify -->
    </style>

    <style name="BottomNavText.Active" parent="BottomNavText">
        <item name="android:textSize">13sp</item>
        <item name="itemIconSize">24dp</item> <!-- Default is 24dp but good to specify --><!-- Slightly larger when active -->
    </style>

    <style name="BottomNavText.Inactive" parent="BottomNavText">
        <item name="android:textSize">12sp</item>
        <item name="itemIconSize">24dp</item> <!-- Default is 24dp but good to specify -->
    </style>


    <style name="Theme.SplashScreen" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.App</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="CampusAppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/background_dark</item>
        <item name="colorOnPrimary">@color/text_light</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_light</item>
        <item name="colorOnSecondary">@color/text_light</item>

        <!-- Background -->
        <item name="android:windowBackground">@drawable/bg_gradient</item>

        <!-- Button styles -->


        <!-- Other theme items -->
        <item name="android:statusBarColor">@color/yellow</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <!-- No ActionBar variant -->
    <style name="CampusAppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- buttons style -->

    <style name="AbsoluteRoundButton" parent="Widget.MaterialComponents.Button">
        <!-- COMPLETELY disable Material's styling -->
        <item name="backgroundTint">@null</item>
        <item name="android:background">@drawable/button_custom</item>
        <item name="cornerRadius">0dp</item> <!-- Disable Material's radius -->

        <!-- Text styling -->
        <item name="android:textColor">#FF000000</item> <!-- Black text -->
        <item name="android:textSize">16sp</item>

        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">24dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
    </style>
</resources>
